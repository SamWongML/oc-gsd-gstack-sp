<!-- opencode-harness -->
# Project rules

This project uses **opencode-harness** for disciplined three-framework coding.
Global harness rules (router, size flow, hops, TDD discipline) live in:

  `~/.config/opencode/AGENTS.md`

opencode merges global + project AGENTS.md automatically. Project-specific
context goes below this line.

---

## Project context

<!-- Add project-specific context here:
- Stack (e.g., Next.js 15 + Postgres + Prisma)
- Conventions (e.g., naming, layering, dependency rules)
- Required commands (e.g., `pnpm dev`, `pnpm test`)
- Files the agent must always read first
-->

## Project-only forbidden patterns

<!-- e.g.,
- Never use `any` in TypeScript.
- Never write directly to `prod_*` tables — go through the repository layer.
-->
